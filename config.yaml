# ServiceNow Agent Configuration

# ServiceNow Settings
snow:
  instance_url: "https://yourcompany.service-now.com"
  approvals_path: "/approvals.do"  # Or "/sysapproval_approver_list.do"
  
# IIS Server Settings
iis:
  base_url: "https://iis-server.company.com"
  asset_search_path: "/asset-search"

# Browser Settings
browser:
  headless: false  # Set to true for production
  slow_mo: 100     # Milliseconds between actions
  timeout: 30000   # Default timeout in ms

# Schedule Settings
schedule:
  enabled: true
  interval_minutes: 5  # Check every 5 minutes
  max_runs_per_day: 10  # Safety limit

# Login Settings
# User will manually login - agent waits for login to complete
login:
  wait_for_login: true
  login_timeout: 120000  # 2 minutes to complete manual login

# Logging
logging:
  log_dir: "./logs"
  log_file: "decisions.csv"
  log_level: "INFO"

# CAD Rules (Placeholder - will be configured later)
cad_rules:
  enabled: false
  rules: []
